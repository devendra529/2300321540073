// vehicle.ts
// this is used to define the structure of the vehicle object that we will be receiving from the database.
export interface Vehicle {
  TaskID: string;
  Duration: number;
  Impact: number;
}